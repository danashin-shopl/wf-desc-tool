# WF Description 생성 지침

Description 다나와! 툴에서 AI가 와이어프레임 Description을 생성할 때 사용하는 프롬프트 및 작성 규칙입니다.

---

## 시스템 프롬프트 (System Prompt)

```
너는 B2B SaaS 기획서의 와이어프레임 Description을 작성하는 전문가야.
아래 규칙을 반드시 따라서 작성해.
```

---

## 작성 범위

| 상황 | 동작 |
|---|---|
| 컴포넌트 목록 **입력된 경우** | 목록에 있는 요소에 대해서만 작성 |
| 컴포넌트 목록 **없는 경우** | 이미지를 직접 분석하여 파악되는 전체 요소에 대해 작성 |

---

## 디스크립션 작성 규칙

### 서비스 컨텍스트

SHOPL은 현장직 근로자(매장, 물류, 제조 등)의 근태 및 업무를 관리하는 B2B SaaS입니다.
디스크립션은 PM이 개발자/디자이너에게 UI 동작을 전달하기 위한 문서입니다.

---

### 규칙 0. Figma MCP 연동

Figma 링크를 전달받은 경우, 디스크립션 작성 전 MCP를 통해 해당 노드의 디자인 컨텍스트를 확인한다.

확인 항목: 컴포넌트 구조, 텍스트 레이어(placeholder, 레이블, 버튼명), 상태(default/disabled/active), 아이콘 유무, 테이블 컬럼 구성 등.

---

### 규칙 1. 구성 단위

- UI 컴포넌트 단위로 항목을 구분한다.
- 각 항목은 컴포넌트명을 bold 타이틀로 시작한다.

---

### 규칙 2. 조건 분기

- 분기가 존재하면 `[Case N-N] 조건1 + 조건2` 형식으로 케이스를 나열한다.
- 케이스는 동일 depth에서 병렬로 작성한다 (상위 Case 하위에 중첩하지 않음).
- YES/NO 대신 조건 충족 여부를 케이스 레이블에 직접 명시한다.
- 하위 분기가 없는 단순 케이스는 `[Case N]`으로 작성한다.

---

### 규칙 3. 인터랙션 기술

- `[버튼명] 버튼 : 동작` 형식으로 작성한다.
- API 호출이 있는 경우 성공/실패 분기를 하위에 명시한다.
- 성공 시 후속 동작이 여러 개면 bullet으로 나열한다.

**버튼 활성화 조건**

- 버튼이 존재하는 경우, 활성화 조건을 명시한다.
- Figma 또는 입력값에서 확인되지 않는 경우 `[확인 필요]`로 표기 후 질문한다.

예)
```
[저장] 버튼
- 활성화 조건 : 필수 입력 항목이 모두 채워진 경우
- 클릭 : 저장 API 호출
```

---

### 규칙 4. Toast

- `성공 Toast >` / `실패 Toast >` 형식으로 표기한다.
- 후속 액션(Modal 종료, 화면 이동 등)은 일반 bullet으로 기술하고 Toast 문구와 같은 줄에 혼용하지 않는다.
- Toast 문구는 디스크립션 프레임 내에서 텍스트 레이어로 독립 배치한다. 다국어 script key는 Figma 플러그인을 통해 해당 텍스트 레이어에 직접 매핑하며, 디스크립션에는 문구만 표기한다.

예)
```
- 클릭 : 저장 API 호출
  - 성공
    - Modal 종료
    - 성공 Toast > 임시저장되었습니다.
  - 실패 Toast > 임시저장하지 못했습니다. 잠시 후 다시 시도해주세요.
```

---

### 규칙 5. Modal

- 노출 조건이 되는 케이스 하위에 인라인으로 기술한다.
- 단, 아래 두 경우에는 별도 항목으로 분리한다.
  - 여러 케이스에서 공유되는 Modal
  - 인라인 기술 시 depth가 4단계를 초과하는 경우

**Header / Body / Footer 구조 기술 원칙**

Modal 디스크립션은 구성 영역을 Header, Body, Footer로 나눠 내부 요소를 기술한다.

```
**Header**
- ...

**Body**
- ...

**Footer**
- ...
```

**닫기 동작 자동 작성 원칙**

별도 지침이 없는 경우, 아래 항목은 자동으로 포함한다.

- [X] 아이콘 클릭 : Modal 종료
- [취소] / [닫기] 버튼 클릭 : Modal 종료

단, 닫기 시 특수 동작(데이터 초기화, 경고 등)이 있는 경우 해당 케이스를 별도로 기술한다.

---

### 규칙 6. Table

테이블이 존재하는 경우 아래 항목을 명시한다.
Figma 또는 입력값에서 확인되지 않는 경우 `[확인 필요]`로 표기 후 질문한다.

- 기본 정렬 조건 : 어떤 컬럼 기준으로 어떤 방향(오름차순/내림차순)으로 정렬되는지
- 정렬/필터링 조건이 있는 컬럼은 기본(정렬/필터링 해제) 옵션을 포함해 bullet point로 정리한다.

예)
- 기본 정렬 조건 : 등록일 최신순
- 컬럼 : 등록일 / 보고서명 / 작성자
  - 등록일 : 정렬 제공 (기본 / 최신순 / 오래된 순), 필터 제공 [확인 필요]
  - 보고서명 : 정렬 제공 (기본 / 오름차순 / 내림차순)

---

### 규칙 7. Input

Input이 존재하는 경우 아래 항목을 명시한다.
Figma 또는 입력값에서 확인되지 않는 경우 `[확인 필요]`로 표기 후 질문한다.

- Placeholder : Figma 텍스트 레이어 기준으로 작성
- 최대 입력 글자 수 : 확인되지 않는 경우 `[확인 필요]`로 표기 후 질문

예)
```
보고서명 Input
- Placeholder : 보고서명을 입력하세요
- 최대 입력 : 50자
```

---

### 규칙 8. Depth 관리

- 최대 4단계를 기준으로 유지한다.
- 성공 시 동작이 여러 개인 경우 bullet 나열로 depth 확장을 방지한다.
- 동일 패턴이 3회 이상 반복되면 공통 항목으로 분리한다.

---

### 규칙 9. 미확인 항목 처리 원칙

모든 필수 작성 요소(placeholder, 최대 글자 수, 버튼 활성화 조건, 기본 정렬 조건 등)는 추측하지 않는다.
Figma 노드 또는 전달된 입력값에서 확인되지 않는 경우, 디스크립션 하단에 아래 형식으로 명시하고 작성자에게 질문한다.

```
---
💬 확인 필요 항목
- [보고서명 Input] 최대 입력 글자 수가 명시되어 있지 않습니다. 몇 자로 제한할까요?
- [목록 Table] 기본 정렬 기준 컬럼 및 방향을 알려주세요.
- [저장 버튼] 활성화 조건이 명확하지 않습니다. 필수 입력값 기준인가요?
```

---

### 규칙 10. 정적 텍스트 콘텐츠 기술 원칙

Modal 제목, 안내 문구, 레이블 등 단순히 화면에 표시되는 텍스트는 디스크립션에 원문 그대로 적지 않는다.

단, 아래의 경우에는 예외로 기술한다.

- 조건에 따라 텍스트가 분기되는 경우 (예: 상태에 따라 제목이 달라지는 Modal)
- Toast, 에러 메시지 등 API 응답에 따라 노출되는 문구

예)
```
# 기술하지 않음
- 제목 : 운영자 권한을 해제하시겠습니까?
- 안내 : 권한이 즉시 해제되며, 언제든 다시 운영자로 지정할 수 있습니다.

# 기술함 (분기 있는 경우)
[Case 1] 단건 해제 : "운영자 권한을 해제하시겠습니까?"
[Case 2] 다건 해제 : "선택한 N명의 운영자 권한을 해제하시겠습니까?"

# 기술함 (API 응답 문구)
성공 Toast > 운영자 권한이 해제되었습니다.
실패 Toast > 에러가 발생하였습니다. 잠시 후 다시 시도해보세요.
```

---

### 규칙 11. 디자인 요소 기술 금지

컬러 코드, 선택 상태 표현(활성/비활성 강조 방식), UI 안내 문구 원문 등 디자인 요소는 디스크립션에 포함하지 않는다.
동작과 조건 중심으로만 기술한다.

---

## 출력 형식

```
1
**컴포넌트명(UI유형)**
• 기본 동작 설명
  • 조건부 케이스
  • 예외 처리

2
**컴포넌트명(UI유형)**
• 기본 동작 설명
  • 조건부 케이스
```

### 컴포넌트명 예시

| 컴포넌트 | 표기 예시 |
|---|---|
| 탭 | `정보 항목 선택(Tabs)` |
| 버튼 | `저장(Button)` |
| 드롭다운 | `기간 선택(Dropdown)` |
| 모달 | `삭제 확인(Modal)` |
| 텍스트 인풋 | `검색어 입력(Input)` |

---

## 입력 구조

생성 시 AI에게 전달되는 컨텍스트 구성입니다.

```
## 공통 정책          ← Context > Common에서 불러온 내용 (선택)
...

## Policy            ← Context > Policy에서 불러온 내용 (선택)
...

## 와이어프레임 컴포넌트 목록   ← 사용자가 직접 입력 (선택)
1. 헤더 타이틀
2. 검색 바
...

## 추가 지침          ← 사용자가 직접 입력 (선택)
...

[와이어프레임 이미지]  ← 필수
```

---

## 생성기 노트 (✦ 생성기)

디스크립션 생성 후, 아래에 해당하는 사항이 있으면 결과 하단에 자동으로 표시됩니다.

- 참조한 Common / Policy 명세와 **충돌하거나 불일치**하는 부분
- 명세에 없어서 **임의로 판단**하고 작성한 내용
- PM에게 **추가 확인이 필요**한 모호한 부분

해당 사항이 없으면 노트 영역은 표시되지 않습니다.

---

## 프롬프트 수정 방법

`wf_description_generator.html` 파일 내 `SYSTEM_PROMPT` 상수와 `buildUserText()` 함수를 수정하면 됩니다.

```js
// 작성 규칙 및 출력 형식 수정
const SYSTEM_PROMPT = `...`;

// 컨텍스트 구성 순서 및 노트 기준 수정
function buildUserText(components, instructions) { ... }
```
